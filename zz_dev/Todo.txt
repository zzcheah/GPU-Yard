=== === === === === === === === === === === === === === === === === === === === === === ===
                                      In Progress
=== === === === === === === === === === === === === === === === === === === === === === ===

Main Server:
Job Scheduling:
-> remove eureka server
-> change architecture to have task worker pool from main server instead

Security:
-> return only jwt with user profile as claim
-> Rest Controller for React (request details, user details)

React:
-> redesign whole front end
-> add administrator use cases


Integration testing:
-> different machine testing (on cloud maybe)
   * cors testing
   * mongodb connections



Additional:
-> notify user when finish processing


*** Critical ***
-> error running container after windows update


=== === === === === === === === === === === === === === === === === === === === === === ===
                                      COMPLETED
=== === === === === === === === === === === === === === === === === === === === === === ===

MLaaS Logic:
-> prebuild functions/scripts
   3.   upload output zip
   4.   call api to release machine along with remarks