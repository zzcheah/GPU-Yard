type User {
    id: String!
    name: String!
    email: String!
    phone: String
    role: String
    status: String
}

extend type Query {
    allUsers: [User]!
}

# Root
#extend type Mutation {
#    registerUser(input: RegisterUserInput): User!
#}
